extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      //- Add a title or heading like My Portfolio, or Hey, Potential Employers, or Let's make the web great together! 
      h1 Davey's Digital Digest

      //- Add a description for your portfolio, what this project is, it's purpose, what it's supposed to accomplish, etc. 
      p.lead.text-light
        | Check out this collection of JavaScript and Python projects!
  article.grid-container.portfolio-index
    h3 JavaScript
    br
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project in JavaScriptProjectList
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`/static/img/project-id-${project.id}/screen-shot-01.png`)
            h5= project.title
    br
    br
    h3 Python
    br
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project in PythonProjectList
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`/static/img/project-id-${project.id}/screen-shot-01.png`)
            h5= project.title
